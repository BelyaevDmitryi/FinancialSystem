# P1 тестовая среда — merge с docker-compose.test.yml + docker-compose.test-p1.yml
#   cp .env.test-p1.example .env.test-p1
#   docker compose -f docker-compose.test.yml -f docker-compose.test-p1.yml --env-file .env.test-p1 up -d
#
# Наследует RM1-поведение mock-брокера (как .env.test.ose).

JWT_SECRET=5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437

BROKER_INTEGRATION_ENABLED=true
TBANK_TOKEN=mock
SPRING_PROFILES_ACTIVE=test

BROKER_ACCOUNT_ID=mock-test-account
