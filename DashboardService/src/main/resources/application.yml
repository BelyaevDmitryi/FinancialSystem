server:
  port: 8009

spring:
  application:
    name: dashboard-service
  cloud:
    loadbalancer:
      enabled: true

logging:
  level:
    root: INFO
  file:
    name: logs/DashboardService.log

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
    healthcheck:
      enabled: true
  instance:
    prefer-ip-address: false
    hostname: dashboard-service
    status-page-url: http://dashboard-service:8009/actuator/info
    health-check-url: http://dashboard-service:8009/actuator/health
    home-page-url: http://dashboard-service:8009/
    lease-renewal-interval-in-seconds: 30
    lease-expiration-duration-in-seconds: 90

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
  info:
    env:
      enabled: true
    app:
      name: ${spring.application.name}
      description: Dashboard Service for Financial System

springdoc:
  api-docs:
    path: /api-docs
